numpy
scipy
pandas
tqdm
pyyaml
pillow
# Headless build: no libGL dependency (required for Streamlit Cloud / Docker slim)
opencv-python-headless
matplotlib
scikit-learn
scikit-image

torch
torchvision
timm

streamlit

open_clip_torch

# Local package (required for `import surfaceguard` on Streamlit Cloud, CI, etc.)
-e .

