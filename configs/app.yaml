app:
  outputs_dir: outputs
  default_dataset_root: data/mvtec_ad
  default_category: bottle
  default_method: patchcore
  # If outputs/patchcore is empty (e.g. fresh Space): sync entire patchcore/** tree from this Hub Model repo.
  hf_patchcore_repo: "harsh9059/surfaceguard-patchcore"

